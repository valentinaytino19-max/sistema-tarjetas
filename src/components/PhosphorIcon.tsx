import {
  Bank,
  MapPin,
  Sun,
  Storefront,
  Building,
  Buildings,
  House,
  HardHat,
  Factory,
  Hospital,
  GraduationCap,
  ShoppingCart,
  CreditCard,
  ChartBar,
  ClipboardText,
  Folder,
  FolderOpen,
  Target,
  Star,
  Fire,
  Lightbulb,
  Plus,
  Image,
  CaretDown,
  CaretRight,
  Pencil,
  Trash,
  ArrowsLeftRight,
  Check,
  Warning,
  MagnifyingGlass,
  ArrowCounterClockwise,
  Package,
  GearSix,
  List,
  SignOut,
  Envelope,
  Palette,
  Smiley,
  Camera,
  type IconProps,
} from '@phosphor-icons/react';

const iconMap: Record<string, React.ComponentType<IconProps>> = {
  Bank,
  MapPin,
  Sun,
  Storefront,
  Building,
  Buildings,
  House,
  HardHat,
  Factory,
  Hospital,
  GraduationCap,
  ShoppingCart,
  CreditCard,
  ChartBar,
  ClipboardText,
  Folder,
  FolderOpen,
  Target,
  Star,
  Fire,
  Lightbulb,
  Plus,
  Image,
  CaretDown,
  CaretRight,
  Pencil,
  Trash,
  ArrowsLeftRight,
  Check,
  Warning,
  MagnifyingGlass,
  ArrowCounterClockwise,
  Package,
  GearSix,
  List,
  SignOut,
  Envelope,
  Palette,
  Smiley,
  Camera,
};

interface PhosphorIconProps {
  name: string;
  className?: string;
}

export default function PhosphorIcon({ name, className = '' }: PhosphorIconProps) {
  const Icon = iconMap[name];
  if (!Icon) return <span className={className}>?</span>;
  return <Icon className={className} />;
}
